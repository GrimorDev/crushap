# Stack-level env vars for docker-compose.yml (Portainer: set these in the
# stack's "Environment variables" editor, not by committing a real .env).

# Required — a long random string. Generate one with: openssl rand -hex 32
JWT_SECRET=

# Optional
API_PORT=3000
CORS_ORIGIN=*
