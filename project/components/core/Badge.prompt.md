Small status label — verified profile, premium tier, distance/new-match flags.
```jsx
<Badge variant="verified" icon={<Icon name="shield-check" size={12} />}>Verified</Badge>
```
Variants: neutral, verified (green), premium (gold), accent (pink gradient).
