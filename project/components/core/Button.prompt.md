The main call-to-action button. Pill-shaped, primary variant uses the pink gradient + glow — reserve it for one action per screen.
```jsx
<Button variant="primary" size="lg">Get Started</Button>
<Button variant="secondary">Not now</Button>
```
Variants: primary (gradient + glow), secondary (outlined surface), ghost (text only). Sizes: sm, md, lg.
