Frosted-glass bottom tab bar, fixed to the screen bottom. Active tab turns pink.
```jsx
<BottomNav active="discover" onChange={setTab} />
```
Fixed tabs: discover, search, matches, chat, profile — do not add/remove tabs, this is the app's full nav.
