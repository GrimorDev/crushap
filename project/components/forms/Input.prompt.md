Text field used in onboarding, search, and settings forms. Focus state glows pink.
```jsx
<Input placeholder="Your name" />
```
Pass an `icon` (e.g. an <Icon/>) to prefix the field, as used in search bars.
